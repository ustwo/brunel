use_frameworks!

def common_pods
  pod 'Moya', '~> 7.0.2'
  pod 'SVProgressHUD', '~> 2.0'
  pod 'SwiftyJSON', '~> 2.4'
end

target 'Brunel iOS' do
  platform :ios, '9.0'
  common_pods
end

target 'Brunel tvOS' do
  platform :tvos, '9.0'
  common_pods
end

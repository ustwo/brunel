use_frameworks!

def common_pods
  pod 'Moya', '~> 8.0'
  pod 'SVProgressHUD', '~> 2.1'
  pod 'SwiftyJSON', '~> 3.1'
end

target 'Brunel iOS' do
  platform :ios, '9.0'
  common_pods
end

target 'Brunel tvOS' do
  platform :tvos, '9.0'
  common_pods
end
